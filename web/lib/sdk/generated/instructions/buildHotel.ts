/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { PANDA_MONOPOLY_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const BUILD_HOTEL_DISCRIMINATOR = new Uint8Array([
  225, 191, 223, 204, 121, 233, 6, 174,
]);

export function getBuildHotelDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BUILD_HOTEL_DISCRIMINATOR);
}

export type BuildHotelInstruction<
  TProgram extends string = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountPlayerState extends string | AccountMeta<string> = string,
  TAccountPropertyState extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountClock extends
    | string
    | AccountMeta<string> = 'SysvarC1ock11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountPlayerState extends string
        ? WritableAccount<TAccountPlayerState>
        : TAccountPlayerState,
      TAccountPropertyState extends string
        ? WritableAccount<TAccountPropertyState>
        : TAccountPropertyState,
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountClock extends string
        ? ReadonlyAccount<TAccountClock>
        : TAccountClock,
      ...TRemainingAccounts,
    ]
  >;

export type BuildHotelInstructionData = {
  discriminator: ReadonlyUint8Array;
  position: number;
};

export type BuildHotelInstructionDataArgs = { position: number };

export function getBuildHotelInstructionDataEncoder(): FixedSizeEncoder<BuildHotelInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['position', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BUILD_HOTEL_DISCRIMINATOR })
  );
}

export function getBuildHotelInstructionDataDecoder(): FixedSizeDecoder<BuildHotelInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['position', getU8Decoder()],
  ]);
}

export function getBuildHotelInstructionDataCodec(): FixedSizeCodec<
  BuildHotelInstructionDataArgs,
  BuildHotelInstructionData
> {
  return combineCodec(
    getBuildHotelInstructionDataEncoder(),
    getBuildHotelInstructionDataDecoder()
  );
}

export type BuildHotelAsyncInput<
  TAccountGame extends string = string,
  TAccountPlayerState extends string = string,
  TAccountPropertyState extends string = string,
  TAccountPlayer extends string = string,
  TAccountClock extends string = string,
> = {
  game: Address<TAccountGame>;
  playerState?: Address<TAccountPlayerState>;
  propertyState?: Address<TAccountPropertyState>;
  player: TransactionSigner<TAccountPlayer>;
  clock?: Address<TAccountClock>;
  position: BuildHotelInstructionDataArgs['position'];
};

export async function getBuildHotelInstructionAsync<
  TAccountGame extends string,
  TAccountPlayerState extends string,
  TAccountPropertyState extends string,
  TAccountPlayer extends string,
  TAccountClock extends string,
  TProgramAddress extends Address = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
>(
  input: BuildHotelAsyncInput<
    TAccountGame,
    TAccountPlayerState,
    TAccountPropertyState,
    TAccountPlayer,
    TAccountClock
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  BuildHotelInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountPlayerState,
    TAccountPropertyState,
    TAccountPlayer,
    TAccountClock
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PANDA_MONOPOLY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    playerState: { value: input.playerState ?? null, isWritable: true },
    propertyState: { value: input.propertyState ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    clock: { value: input.clock ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.playerState.value) {
    accounts.playerState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 108, 97, 121, 101, 114])),
        getAddressEncoder().encode(expectAddress(accounts.game.value)),
        getAddressEncoder().encode(expectAddress(accounts.player.value)),
      ],
    });
  }
  if (!accounts.propertyState.value) {
    accounts.propertyState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 114, 111, 112, 101, 114, 116, 121])
        ),
        getAddressEncoder().encode(expectAddress(accounts.game.value)),
        getU8Encoder().encode(expectSome(args.position)),
      ],
    });
  }
  if (!accounts.clock.value) {
    accounts.clock.value =
      'SysvarC1ock11111111111111111111111111111111' as Address<'SysvarC1ock11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.playerState),
      getAccountMeta(accounts.propertyState),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.clock),
    ],
    data: getBuildHotelInstructionDataEncoder().encode(
      args as BuildHotelInstructionDataArgs
    ),
    programAddress,
  } as BuildHotelInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountPlayerState,
    TAccountPropertyState,
    TAccountPlayer,
    TAccountClock
  >);
}

export type BuildHotelInput<
  TAccountGame extends string = string,
  TAccountPlayerState extends string = string,
  TAccountPropertyState extends string = string,
  TAccountPlayer extends string = string,
  TAccountClock extends string = string,
> = {
  game: Address<TAccountGame>;
  playerState: Address<TAccountPlayerState>;
  propertyState: Address<TAccountPropertyState>;
  player: TransactionSigner<TAccountPlayer>;
  clock?: Address<TAccountClock>;
  position: BuildHotelInstructionDataArgs['position'];
};

export function getBuildHotelInstruction<
  TAccountGame extends string,
  TAccountPlayerState extends string,
  TAccountPropertyState extends string,
  TAccountPlayer extends string,
  TAccountClock extends string,
  TProgramAddress extends Address = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
>(
  input: BuildHotelInput<
    TAccountGame,
    TAccountPlayerState,
    TAccountPropertyState,
    TAccountPlayer,
    TAccountClock
  >,
  config?: { programAddress?: TProgramAddress }
): BuildHotelInstruction<
  TProgramAddress,
  TAccountGame,
  TAccountPlayerState,
  TAccountPropertyState,
  TAccountPlayer,
  TAccountClock
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PANDA_MONOPOLY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    playerState: { value: input.playerState ?? null, isWritable: true },
    propertyState: { value: input.propertyState ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    clock: { value: input.clock ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.clock.value) {
    accounts.clock.value =
      'SysvarC1ock11111111111111111111111111111111' as Address<'SysvarC1ock11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.playerState),
      getAccountMeta(accounts.propertyState),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.clock),
    ],
    data: getBuildHotelInstructionDataEncoder().encode(
      args as BuildHotelInstructionDataArgs
    ),
    programAddress,
  } as BuildHotelInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountPlayerState,
    TAccountPropertyState,
    TAccountPlayer,
    TAccountClock
  >);
}

export type ParsedBuildHotelInstruction<
  TProgram extends string = typeof PANDA_MONOPOLY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    game: TAccountMetas[0];
    playerState: TAccountMetas[1];
    propertyState: TAccountMetas[2];
    player: TAccountMetas[3];
    clock: TAccountMetas[4];
  };
  data: BuildHotelInstructionData;
};

export function parseBuildHotelInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedBuildHotelInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      game: getNextAccount(),
      playerState: getNextAccount(),
      propertyState: getNextAccount(),
      player: getNextAccount(),
      clock: getNextAccount(),
    },
    data: getBuildHotelInstructionDataDecoder().decode(instruction.data),
  };
}
